## 2026/04/03 Day02学习日报
+  学习时长：6小时
+  学习内容： 
   +  算法：
      +  检测是否存在重复元素
      +  有效的字母异位词
   +  css图片效果
      +  透明，遮罩
      +  模态框
   +  网页布局部分
   +  input输入框样式
      +  边距，颜色,宽高
   +  css3动画
   +  文本动画演示
   +  弹性布局
      +  flex
   +  媒体查询
   +  响应式导航栏
   +  底部分页按钮
   +  网格布局
+  心得与收获(总结学习成果，要详细说明对相关知识点的个人理解.):
   +  通过html结合js完成图片模拟框，利用document获取Dom文档节点对节点进行设置，配合css的伪元素实现遮罩，获取节点元素，通过函数调用设置属性实现页面的动态渲染
   +  算法：map和set都是属性值唯一,可以用来检测列表重复元素或者剔除，方法比较简单，都是键值对形式，都是利用has()检测值是否存在集合里面,利用for...of进行迭代
      +  Set使用add()方法插入元素，map使用set(key,value)方式插入键值对
   +  flex布局实现
      +  行列形式的，有分散，左起始，右起始多种方式，通过对父元素设置达到子元素排列
   +  grid网格布局与flex布局都是通过对子列的定位实现操作布局，但是flex对于多个子列排列比较明显
   +  grid是网格化定位设置，box-sizing实现盒子宽高与实际宽高相等，去除内外边距的影响
   +  下拉菜单导航栏与底部分页按钮都是利用布局实现对元素盒子排列，在进行伪元素操作布局,底部分页利用行内块元素特性排列响应式导航栏利用定位准确实现下拉式菜单,结合元素display属性设置实现元素隐藏和显示