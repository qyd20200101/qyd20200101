## 2026/04/05 Day04学习日报
+  学习时长：6小时
+  学习内容：
   +  算法
      +  数组去重
         +  Set
         +  filter和indexOf
      +  前k个高频元素
      +  最长连续序列
   +  手写防抖
   +  闭包和this指向demo
   +  浏览器离线资源获取
   +  重绘和回流
+  心得与收获(总结学习成果，要详细说明对相关知识点的个人理解.):
   +  通过闭包实现循环从外部访问内部变量，但是会造成变量一直存在，可能会内存泄漏
   +  this指向，默认全局对象window，对象指向它的实例对象，构造函数指向新实例
   +  箭头函数没有形式参数，也没有arguments,无this
      +  指向外部作用域
   +  原型链
      +  每个函数都有`prototype`属性，指向原型对象，每个对象都有`__proto__`指向其构造函数的原型，对象通过`__proto__`向上查找属性和方法，终点为Null
      +  继承是通过原型链实现属性方法共享，obj.prototype.属性,节约内存，实现复用
   +  防抖就是延迟执行，再延迟内再次触发就重置计时
      +  通过清除定时器，设置新定时器，利用apply绑定this，再通过事件监听实现功能