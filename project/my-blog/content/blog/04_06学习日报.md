## 2026/04/06 Day05学习日报
+  每天提交日报，日报格式：
+ 学习时长： 6小时
+  学习内容：  
   + HTML语义化
   + BFC
   +  居中布局
      + flex
      + 定位+transform
      + line-height实现文字行内元素垂直居中，仅用于单行文字
   +  盒模型
      + content-box标准盒模型
      + border-box区别在于width是否包含border+padding
   +  堆，栈
      + Js中基本类型存栈，引用类型存堆（栈仅存堆的引用地址）
   +  js数据类型
      + 基本数据类型，引用数据类型
      + typeof判断数据类型
      + null返回为object是因为内存地址和object设定相同
      +  检查是否为数组的方法
         + instanceof
         + object.prototype.toString.call
      +  深拷贝&浅拷贝
         + 深拷贝遍历复制方法属性
         + 处理嵌套对象，循环引用，特殊类型判断
         + 浅拷贝只复制一层
      +  防抖节流
         +  防抖，多次操作只返回最后一次
            + 搜索框输入
         +  节流，一定时间内只返回一次
            + 滚动加载，resize获取
      +  数组去重
         + Set+解构
         + filter+indexOf
      +  闭包
         + 函数内部访问外部作用域变量,外部函数执行后变量不被垃圾回收
         + 循环遍历使用var出错
         + 使用let块级作用域
         + 闭包包裹，通过立即执行函数创建闭包,保存循环中每次迭代的变量
      +  原型链和原型
         + 每个函数都有`prototype属性(原型对象)`,每个对象的`__proto__`指向构造函数的`  prototype`,`构造函数.prototype.comstructor`指向构造函数本身,通过`__proto__`沿着原型对象查找属性和方法
+  心得与收获： 总结学习成果，要详细说明对相关知识点的个人理解。
   +  BFC的核心是布局隔离，实际开发可以解决浮动导致的父容器高度塌陷问题
   +  闭包是通过内部访问外部的变量，实现节约资源，但是闭包也会导致变量一直存在，可能内存泄露
   +  原型，原型链：实例对象通过`prototype`获取方法和属性
   +  实例对象的`__proto__`指向构造函数的`prototype`(隐式调用)
   +  语义化标签可以帮助seo搜索，并且帮助结构内容清晰，浏览器排行和爬虫语义化排名靠前
   +  typeof检测基本数据类型
      +  检测null为object是因为设计时，null的地址和object地址都是00,00x0
      +  检测数组,instanceof，Object.prototype.toString.call检测对象，Array.isArray专门判断数组
      +  instanceof Aray存在跨窗口/iframe判断失效,Object.prototype.toString.call通用
+ 遇到的问题 (可选)： 有什么问题