## 2026/04/10 Day09学习日报
+ 每天提交日报，日报格式：

+ 学习时长： 7小时

+ 学习内容： 

  +  手写promise.all
  +  Promise.all
     + 并发请求
  +  Promise.race
     + 处理超时请求
  +  Promise.allSettled
     + 获取成功失败结果数量
  +  axios封装
  +  server后端联调
  +  跨域

+ 心得与收获： 总结学习成果，要详细说明对相关知识点的个人理解。

  + 手写promise.all对参数进行校验，通过顺序存入成功结果，比较存入数量和任务长度判断执行完毕，只要有一个失败,就reject

  + promise.all返回所有成功的子结果，但是如果其中有失败的，则会立即返回第一个失败情况，虽然其他子认为继续执行，但获取不到结果,可以对非重要子任务.ctah局部捕获防止整个任务崩溃

  + promise.allSettled获取子任务成功和失败的数量，提供精确反馈

  + promise.race处理请求超时逻辑，它只是忽略了慢的结果，不能真正取消http请求

    通过配合AbortController来真正终止请求

  +  封装axios在响应拦截器通过response.data.data进行处理，拿到业务数据,拦截器统一对http状态码处理判断，

  +  利用vite通过rewrite实现跨域端口转发

+ 遇到的问题 (可选)： 有什么问题