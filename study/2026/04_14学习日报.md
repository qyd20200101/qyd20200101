## 2026/04/14 Day13学习日报
+  每天提交日报，日报格式：
+ 学习时长： 7小时
+  学习内容： 学习的知识点
   + 封装Hooks
   + 重构代码逻辑
   + TS类型检测
   + 前后端数据返回不一致处理
   + 前后端联调URL地址冲突
   + 编译器代码污染错误引入同名变量
   + ui溢出和性能优化
   + element-plus按需引入
   + 组件二次封装
   + 用户信息的增删查改
+  心得与收获： 总结学习成果，要详细说明对相关知识点的个人理解。
   + 后端数据服务搭建，前端接口层创建对接，组件封装hook将接口层函数当作参数引入，业务层调用,减少模板组件方法的重复定义，Hook内部实现逻辑方法，组件只需要声明绑定关系，从后端到页面展示的数据都是强类型的，减少数据缺失或不符合的错误
   + 前后端联调url地址报错，检测api接口层和后端服务端，逐步排查
   + 表格溢出和性能优化通过设置max-width和overflow滚动条实现页面滚动展示
   + 依赖按需引入优化代码打包体积，Vite编译阶段检查代码发现使用依赖包组件就自动插入依赖，二次封装将逻辑与组件ui展示隔离开
   + 用户信息增删查改通过设置异步锁防止用户重复点击导致后端权限冲突，ui契约通过真假判断控制弹窗显示隐藏，减少状态出错
+ 遇到的问题 (可选)： 有什么问题