## 2026/04/21 Day19学习日报
+  每天提交日报，日报格式：
+ 学习时长： 7小时
+ 学习内容： 学习的知识点
    + 八股文理论： V8 引擎垃圾回收机制（新生代/老生代）、数据类型与堆栈内存、闭包导致内存泄漏的原理。
    + 低代码引擎进阶与修复：打通了动态表单构建器（Form Builder）的右侧属性配置双向绑定，完善了基于 JSON Schema 生成 Vue 3 源码与模板的“出码引擎”。
    + 计算机网络（TCP协议）：深度复习了 TCP 三次握手（建立连接）与四次挥手（断开连接）的底层逻辑与状态扭转。
    + 手写源码（JS进阶）：利用 Array.prototype.reduce 结合递归，手写实现了支持自定义深度的数组扁平化函数 myFlat。

    + 手写与算法： 防抖（Debounce）、节流（Throttle）的核心逻辑、深拷贝（Deep Clone）的循环引用处理。
+ 心得与收获： 总结学习成果，要详细说明对相关知识点的个人理解。
    + 今天在完成低代码引擎出码功能时，真正感受到了 UI = f(State) 的魅力。我们不再手写几十个表单 HTML，而是通过维护一个 JSON 数组，配合 Vue 的 <component :is> 动态渲染。出码引擎本质上也是对这棵 JSON 树的遍历与字符串拼接。

    + 网络协议的通俗化理解：不再死记硬背 SYN 和 ACK，而是从“全双工通信”的角度理解了 TCP。三次握手的核心是为了“验证客户端和服务端双方的收发能力都正常”；四次挥手则是因为挂断时，可能还有一方的“数据没传完”，需要一个半关闭的缓冲状态。
+ 遇到的问题 (可选)： 有什么问题
    + 手写代码时，逻辑框架是对的，但存在大量致命的基础语法错误（如 typeof 拼写、参数位置错乱、箭头函数语法混用、对象属性赋值错误）。
    + 理论表述不够体系化： 知道原理，但在给面试官口述时，专业术语的使用存在倒置或混淆（例如把栈内存和堆内存的作用记反，把闭包的引用方向说反）。