## 2026/05/12 Day14 Vue3深度学习日报

+ 学习时长：1.5 小时

+ 学习内容：
  - 生命周期完整顺序巩固：父子+KeepAlive+指令三个维度组合 → 父 beforeMount → 子 beforeMount → 子 mounted → 指令 mounted → 子 onActivated → 父 mounted；KeepAlive 切回只触发 onActivated，无 mount/unmount
  - 手动实现 v-model：v-model="x" = :modelValue="x" + @update:modelValue="val => x = val"；v-model:title 同理
  - Suspense 异步组件：异步组件的占位机制，<Suspense> 的 #default / #fallback 双插槽
  - defineAsyncComponent：异步组件高级配置，loader/loadingComponent/errorComponent/delay/timeout
  - Teleport：渲染到 DOM 任意位置，<Teleport to="body"> 解决弹窗被父容器 overflow/z-index 切断问题

+ 心得与收获：
  今天状态差，大量的"没听过"——defineAsyncComponent、Suspense、Teleport 都是第一次接触。生命周期虽然在昨天讲清楚了，但今天重新默写时又乱了，说明还没形成肌肉记忆。手动实现 v-model 基本掌握，能写出 :modelValue + @update:modelValue 的完整写法。最大的教训是：昨天掌握的"用户输入→DOM 更新"链路今天又忘了一部分，需要每天重复走一遍固定流程才能形成长期记忆。

+ 遇到问题：
  - 生命周期父子+KeepAlive+指令组合顺序仍然会搞混
  - 5 月 11 号能完整说出的"用户输入→DOM 更新"链路今天部分遗忘
  - defineAsyncComponent / Suspense / Teleport 初次接触，需要实操练习