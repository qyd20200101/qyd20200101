# 2026-05-12 Vue3 底层源码学习 — 自我总结

> 掌握度：🟢真懂 / 🟡模棱两可 / 🔴没记住

---

## 一、我真正掌握的内容（🟢）

- 手动实现 v-model：`:modelValue="x"` + `@update:modelValue="val => x = val"`，`v-model:title` = `:title` + `@update:title`

---

## 二、我模棱两可的内容（🟡）

- 用户输入 → DOM 节点更新完整链路："有点忘记了" —— 5 月 11 号能完整说出来，今天需要重新巩固

---

## 三、我没记住的内容（🔴）

- **生命周期和钩子函数**：具体过程没搞明白，父子+KeepAlive+指令的组合顺序混淆
- **KeepAlive 的 onActivated/onDeactivated**：切走切回只会触发一个钩子，组件实例不销毁
- **defineAsyncComponent**：没听过——异步组件的高级配置写法（loading/error/timeout/delay）
- **Suspense**：没听过——包裹异步组件统一管理 loading 状态的占位机制
- **Teleport**：没听过——把组件传送到 DOM 树任意位置（如 `<body>`）

---

## 四、今天纠正的错误

- 生命周期顺序想当然地排列，未遵循"父开门→子全套→父关门"的规律
- 认为异步组件就是 defineAsyncComponent，实际 `() => import(...)` 已经是异步组件

---

## 五、明天的目标

1. 重新巩固生命周期：用一张纸画出父子+KeepAlive+指令的完整钩子触发顺序图
2. 重新走一遍"用户输入 → DOM 更新"链路（5 月 11 号能做到，今天必须恢复）
3. 练习 defineAsyncComponent 的完整配置写法和 Suspense 的 fallback 用法